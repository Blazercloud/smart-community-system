# 东软智慧社区项目 - 快速启动指南

## 📋 项目概述

东软智慧社区平台，前后端分离架构，包含用户端、管理端、商城模块和社区服务模块。

## 🚀 快速开始

### 前置条件

确保已安装以下软件：
- ✅ JDK 17+
- ✅ Maven 3.6+
- ✅ MySQL 8.0+
- ✅ Redis 6.0+
- ✅ Node.js 18+
- ✅ npm 或 yarn

### 第一步：启动数据库和Redis

#### 1. 启动MySQL
```bash
# Windows
net start mysql

# Linux/Mac
sudo systemctl start mysql
```

#### 2. 启动Redis
```bash
# Windows
redis-server

# Linux/Mac
sudo systemctl start redis
```

### 第二步：创建数据库

```bash
# 进入后端目录
cd back-end-java

# 执行SQL脚本
mysql -u root -p < init.sql
```

或者使用MySQL客户端工具导入 `back-end-java/init.sql` 文件。

**默认测试数据：**
- 管理员账号：`admin` / `admin123`
- 用户账号：`13800138001` / `user123`

### 第三步：配置后端

编辑 `back-end-java/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    username: root
    password: your_password  # 修改为你的MySQL密码
  
  data:
    redis:
      host: localhost
      port: 6379
```

### 第四步：启动后端

```bash
# 进入后端目录
cd back-end-java

# 使用Maven启动
mvn spring-boot:run

# 或使用IDE直接运行 CommunityApplication.java
```

**后端启动成功标志：**
```
=================================
东软智慧社区平台启动成功！
API地址: http://localhost:8080/api
=================================
```

### 第五步：启动前端

打开新的命令行窗口：

```bash
# 进入前端目录
cd front-end-vue

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

**前端启动成功标志：**
```
  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## 🎯 访问应用

### 用户端
1. 访问：http://localhost:5173/login
2. 选择"用户登录"标签
3. 输入测试账号登录

### 管理端
1. 访问：http://localhost:5173/login
2. 选择"管理员登录"标签
3. 输入管理员账号登录

## 📱 功能演示

### 用户端功能
- ✅ 用户注册/登录
- ✅ 个人信息查看
- ✅ 我的钱包
- ✅ 商城购物
- ✅ 社区公告查看
- ✅ 社区服务

### 管理端功能
- ✅ 管理员登录
- ✅ 用户管理
- ✅ 门店管理
- ✅ 商品管理
- ✅ 订单管理
- ✅ 数据统计

## 🔧 常见问题

### 1. MySQL连接失败
**问题：** `Communications link failure`

**解决：**
- 检查MySQL服务是否启动
- 检查用户名密码是否正确
- 检查数据库是否存在

### 2. Redis连接失败
**问题：** `Unable to connect to Redis`

**解决：**
- 检查Redis服务是否启动
- 检查Redis端口是否为6379

### 3. 前端无法连接后端
**问题：** `Network Error`

**解决：**
- 检查后端是否正常启动
- 检查 `src/utils/request.js` 中的baseURL配置
- 检查CORS配置

### 4. 端口被占用
**问题：** `Port 8080 is already in use`

**解决：**
- 修改 `application.yml` 中的端口号
- 或停止占用端口的进程

## 📝 API接口测试

可以使用以下curl命令测试API：

### 用户登录
```bash
curl -X POST http://localhost:8080/api/user/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138001","password":"user123"}'
```

### 管理员登录
```bash
curl -X POST http://localhost:8080/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

## 📚 项目结构

```
10-27-course-design/
├── back-end-java/          # 后端项目
│   ├── src/
│   ├── pom.xml
│   ├── init.sql           # 数据库初始化脚本
│   └── README.md
├── front-end-vue/          # 前端项目
│   ├── src/
│   ├── package.json
│   └── README.md
└── START.md               # 本文件
```

## 🎓 开发说明

### 后端开发
- 统一使用 `Result<T>` 作为响应体
- 使用SLF4J记录日志
- 遵循RESTful API设计
- 使用JWT进行身份认证

### 前端开发
- 使用Element Plus组件库
- 使用Pinia进行状态管理
- 遵循Vue 3 Composition API规范
- API请求统一使用Axios封装

## 📞 技术支持

如有问题，请查看：
- 后端文档：`back-end-java/README.md`
- 前端文档：`front-end-vue/README.md`
- 数据库文档：`back-end-java/DATABASE.md`

---

**祝开发顺利！** 🎉

